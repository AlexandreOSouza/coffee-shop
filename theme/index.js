import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    colors: {
        primary: '#FF902A',
        secondary: '#2F2105',
        light: '#F9D9AA',
    }
})

export default theme
